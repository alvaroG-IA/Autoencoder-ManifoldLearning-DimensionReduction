import numpy as np

class Autoencoder:
    def __init__(self, epochs: int, loss_threshold: float):
        self.epochs = epochs
        self.loss_threshold = loss_threshold
    
    def fit(self, data: np.narray):
        pass

    def transform(self, data: np.narray):
        pass